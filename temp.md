// Function to check if a number is prime\nfunction isPrime(number) {\n  // Handle edge cases for numbers less than 2\n  if (number <= 1) {\n    return false;\n  }\n\n  // Optimization: Check divisibility only up to the square root of the number\n  for (let i = 2; i <= Math.sqrt(number); i++) {\n    // If the number is divisible by any number between 2 and its square root, it's not prime\n    if (number % i === 0) {\n      return false;\n    }\n  }\n\n  // If no divisors were found, the number is prime\n  return true;\n}\n\n// Example usage:\nconst num = 29;\nif (isPrime(num)) {\n  console.log(num + \" is a prime number\");\n} else {\n  console.log(num + \" is not a prime number\");\n}\n\n// Export the function to be used in other modules (e.g., in a Node.js environment)\nmodule.exports = isPrime;"